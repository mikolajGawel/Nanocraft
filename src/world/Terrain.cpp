#include "Terrain.hpp"
Terrain::Terrain(unsigned int seed,TerrainSettings terrainSettings):
    seed(seed),terrainSettings(terrainSettings)
{
}

int Terrain::getTerrainHeight(glm::ivec2 position_xz){
    
}